# 工作台迁移包说明（2026-08-16）

## 这是什么
本压缩包包含“公寓管理工作台”的完整文件，可直接复制到台式机/服务器上使用。

## 包内内容
- ams_proxy.py —— 后端代理服务（端口 8899）
- ams_system.html / ams_login.html —— 前端页面
- echarts.min.js + maps/ —— 地图组件（中国/内蒙古/呼和浩特）
- finance.db —— SQLite 数据库（收入流水 + 每日在住快照 + 布草等全部历史数据）
- cache/ams_token.txt —— AMS 登录凭证（可能过期，过期后需重新扫码登录）
- 抓取/对比脚本、启动/停止脚本、说明文档、.gitignore

## 本次对话摘要（2026-08-16）
问题：在住数据只更新到 8/13，之后没继续更新。
原因：自动更新任务（每天 7:00 抓在住快照）存在且启用，但 8/14–8/16 早上电脑都不在可运行状态：
- 8/14 早上电脑关机中（8/14 上午 10:11 才开机）
- 8/15 早上电脑在睡眠（8/14 晚上合盖休眠后未唤醒）
- 8/16 早上电脑因电池耗尽关机（当天 14:04 才开机）
- 8/16 14:09 开机后有一次补跑尝试，被任务设置拒绝（任务要求“仅插电 + 仅登录时运行”）
结论：任务本身没问题，是笔记本不常开 + 睡眠/电池导致断更。8/14、8/15 的历史快照无法回补（脚本只能抓当前在住名单），8/16 可手动补跑。
方案：改用台式机作为常开“服务器”，并配置远程访问。

## 台式机部署步骤
1. 安装 Python（建议与现在一致：Python 3.14，默认路径 `%LOCALAPPDATA%\Programs\Python\Python314`）
2. 解压本包到台式机（建议 `C:\工作台` 或 `D:\工作台`）
3. 若 Python 版本/路径不同，请同步修改 start_workbench.ps1、自动更新.ps1 中的 Python 路径
4. 双击“启动工作台.bat”启动，浏览器打开 http://127.0.0.1:8899/ams_system.html
5. 若提示登录，打开 http://127.0.0.1:8899/ams_login.html 重新扫码登录
6. 按下方说明重建计划任务

## 计划任务设置建议
原任务清单：
- WorkBench Residents Auto Update —— 每天 7:00（在住快照）
- WorkBench Income Update 08AM / 02PM / 1130PM —— 每天 3 次（收入流水增量）
- WorkBench Board Monthly Update —— 每月 1 日 8:30（AMS 看板）
在台式机上重建时建议：
- 勾选“错过计划启动时间后尽快运行”（补跑）
- 台式机保持开机并登录即可，不强制要求；若要“用户未登录也运行”，需管理员权限并保存密码
- 台式机无电池问题，电源相关条件可忽略

## 远程访问（不在局域网时）
1. 推荐：Tailscale / ZeroTier 组网——手机、电脑装客户端，随时随地像局域网一样访问，免费且安全
2. 备选：花生壳 / cpolar 内网穿透——给外人直接发网址，但必须加访问密码 + HTTPS
3. 最稳：部署到阿里云/腾讯云轻量服务器（约几十元/月），天然公网可访问
4. 无论哪种方式，防火墙需放行 8899 端口

## 注意事项
- finance.db 含敏感财务数据，请定期备份到其它设备
- 不要把 8899 端口直接暴露公网（除非已配好密码 + HTTPS）
- 自动更新日志写在 logs/ 目录，出问题先看当天日志
- 本包不含 .git 历史；如需与 GitHub 同步，请单独 git clone 后再覆盖文件

---

## 对话迁移与备份（2026-08-16 补录）
本包同时包含本次排查对话的完整记录，方便回顾和迁移到新电脑继续聊。

- 对话记录_20260816.md —— 可读版对话全文（31 条消息，已去除系统噪音）
- 对话备份/rollout-2026-08-16T14-39-02-01a0094b-7c01-7491-b4a3-9da0c74d5994.jsonl —— Codex 原始会话数据

**把对话迁移到另一台电脑继续聊：**
1. 关闭新电脑上的 Codex
2. 把 `对话备份\rollout-*.jsonl` 复制到新电脑的
   `%APPDATA%\xin-buddy\codex-home\sessions\2026\08\16\` 目录（没有该目录就新建）
3. 重新打开 Codex，这条对话就会出现在历史列表里，可直接继续

注意：`.md` 文档只是文字备份，要“接着聊”必须复制 `.jsonl` 会话文件。

---

## 定时任务重建（2026-08-16 补录）
原电脑的 5 个计划任务不会随压缩包自动迁移，本包已附带一键重建脚本：

- 重建计划任务.ps1 —— 双击或右键“使用 PowerShell 运行”即可重建全部任务（建议以管理员运行）

重建后任务清单：
| 任务名 | 时间 | 内容 |
| --- | --- | --- |
| WorkBench Residents Auto Update | 每天 07:00 | 在住快照 |
| WorkBench Income Update 08AM | 每天 08:00 | 收入流水增量 |
| WorkBench Income Update 02PM | 每天 14:00 | 收入流水增量 |
| WorkBench Income Update 1130PM | 每天 23:30 | 收入流水增量 |
| WorkBench Board Monthly Update | 每月 1 日 08:30 | AMS 看板 |

所有任务都勾选了“错过计划启动时间后尽快运行”（StartWhenAvailable），台式机开机即会补跑。
